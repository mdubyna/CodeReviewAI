services:
  app:
    build: .
    command: uvicorn app:app --host 0.0.0.0 --port 8000
    ports:
      - "8000:8000"
    volumes:
      - ./:/app
    env_file:
      - .env

  redis:
    image: "redis:alpine"
    ports:
      - "6379:6379"
